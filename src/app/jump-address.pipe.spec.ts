import { JumpAddressPipe } from './jump-address.pipe';

describe('JumpAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new JumpAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
